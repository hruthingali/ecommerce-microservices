spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/postgres  # ✅ Change `localhost` to `postgres`
    username: postgres
    password: root
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true
